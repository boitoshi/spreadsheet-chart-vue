name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  dashboard:
    name: portfolio-dashboard (型チェック・テスト・ビルド)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Node をセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: portfolio-dashboard/package-lock.json

      - name: 依存関係をインストール
        working-directory: portfolio-dashboard
        run: npm ci

      - name: 型チェック（server + client）
        working-directory: portfolio-dashboard
        run: npm run check

      - name: テスト（server + client）
        working-directory: portfolio-dashboard
        run: npm run test

      - name: ビルド
        working-directory: portfolio-dashboard
        run: npm run build

  collector:
    name: collector (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: uv をセットアップ
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: ruff lint
        working-directory: portfolio-dashboard/collector
        run: uv run ruff check .
